# VIKI Dashboard - Copy to .env and set your own values.
# Never commit .env.

# API key (must match VIKI_API_KEY used by the backend)
VITE_VIKI_API_KEY=

# Backend API base URL (default when running locally)
VITE_VIKI_API_BASE=http://localhost:5000/api
