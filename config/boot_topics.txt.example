# Copy to your VIKI data directory as boot_topics.txt (see forge.boot_topics_file).
# One DuckDuckGo search query per line. Used when forge.background_evolution_at_boot is true.

# climate policy updates 2026
# local LLM trends
