[pytest]
testpaths = tests
pythonpath = .
addopts = -ra
markers =
    live: hits a real server; set QA_LIVE_API=1, QA_BASE_URL, QA_API_KEY (see README)
    smoke: minimal fast checks for live environments
